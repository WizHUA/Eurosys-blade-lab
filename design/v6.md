# HPC-Diagnosis Agent Design (v6)

本文是 HPC-Diagnosis Agent 的完整系统设计规格，面向 SC'26 论文写作。v6 在 v5 基础上完成一次关键架构升级：将 GATE 从纯规则节点升级为具备主动审查能力的 Adversarial Sub-Agent。版本变更历史见 `design/changelog.md`。

---

## Part I：设计原则

1. **两阶段清晰分离**：Triage 与 Diagnosis Agent 职责独立，便于消融与复现
2. **双角色对抗审查**：THINK 负责诊断推进，GATE 负责独立质疑，防止自我背书
3. **零人工阈值**：不在运行时以硬编码阈值决定结论是否成立，证据质量由 GATE LLM 自主判断
4. **多假设并行跟踪**：显式维护多个候选根因及其各自证据链
5. **工具输出摘要化**：原始时序不直接进入长上下文，避免 token 爆炸
6. **规则库有 Schema**：FPL 只能按固定 JSON Schema 回写，防止知识污染
7. **可审计执行轨迹**：Diagnosis Trace 与 Gate Trace 分离记录，便于分析失败模式

---

## Part II：系统架构

### 2.1 输入 / 输出

| 方向 | 内容 | 格式 |
|------|------|------|
| 输入 | `metrics.csv` — 时序指标矩阵（~150 列） | CSV，时间戳索引 |
| 输入 | `jobinfo.csv` — Slurm 作业信息 | CSV |
| 输出 | `DiagnosisReport` — 根因列表、证据链、解决建议 | JSON |
| 输出 | `ExecutionTrace` — 主 Agent + GATE Sub-Agent 轨迹 | JSON |

> `chaos.csv` 仅用于评估框架（Part VIII），不参与诊断流程。

### 2.2 三阶段架构图

```text
输入：metrics.csv + jobinfo.csv
                │
                ▼
┌─────────────────────────────────────────┐
│  Offline Knowledge Base                 │
│  ┌─────────────┐ ┌──────────────────┐   │
│  │  Metric KB  │ │ Fault Pattern    │   │
│  │ (YAML+向量)  │ │ Library (JSONL)  │   │
│  └─────────────┘ │ （初始为空）       │   │
│                   └──────────────────┘   │
└─────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Stage 1: Triage（确定性，纯 Python）    │
│  Step 1 异常筛选 → Step 2 时序排序 →     │
│  Step 3 FocusContext 构建                │
└──────────────────┬──────────────────────┘
                   │ FocusContext
                   ▼
┌───────────────────────────────────────────────────────────┐
│  Stage 2: Diagnosis Agent（LangGraph）                    │
│                                                           │
│  HYPOTHESIZE（LLM）                                        │
│     │                                                     │
│     ▼                                                     │
│  ┌──────────── Main ReAct Loop ────────────┐              │
│  │ THINK ─[tool_call]─→ ACT ─→ OBSERVE ───┘              │
│  │   │                                                    │
│  │   └────────────[conclude]──────────────┐              │
│  ▼                                        ▼              │
│  GATE Sub-Agent（Adversarial LLM Reviewer）               │
│    GATE_THINK ─[optional tool_call]→ GATE_ACT → GATE_OBS │
│        │                                                  │
│        └──────────── structured decision ──────────────┐  │
│             ├─[pass]──────────────→ FINALIZE           │  │
│             ├─[continue + hint]───→ THINK              │  │
│             ├─[rehypothesize]─────→ HYPOTHESIZE        │  │
│             └─[degrade]───────────→ FINALIZE           │  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Stage 3: Reflect                       │
│  规则提炼 → 去重写入 → FPL 回写          │
└─────────────────────────────────────────┘
                   │
                   ▼
输出：DiagnosisReport (JSON) + ExecutionTrace
```

### 2.3 与 STELLAR (SC'25) 的对比定位

| 维度 | STELLAR (SC'25) | 本系统 |
|------|-----------------|--------|
| 应用目标 | I/O 参数自动调优 | 故障根因诊断 |
| 推理控制 | LLM 自由生成 + 反思总结 | **确定性 Triage** + 双角色 LLM Agent |
| 证据验证 | 无独立审查者 | **Adversarial Evidence Gating** |
| 多故障支持 | 单目标优化 | **多假设并行**，支持复合故障 |
| 知识库 | RAG 检索文档片段 | **结构化 FPL**（含状态、版本、来源追踪） |

### 2.4 核心贡献点

1. **Hybrid Control**：确定性 Triage 压缩 context、提供 causal_order，主 Diagnosis Agent 负责聚焦推理
2. **Adversarial Evidence Gating**：THINK 与 GATE 角色分离、信息隔离，GATE 以对抗审查者身份主动寻找漏洞，实现零人工阈值的证据门控
3. **Fault Pattern Library**：从空开始，通过 Reflect 自动积累的结构化故障知识库
4. **多假设并行 + 继发症状甄别**：显式维护 N 个候选根因，并区分复合故障与下游症状链

### 2.5 论文 Claim

> 完全自主的 HPC 故障诊断 Agent 框架：零人工阈值、零标注数据、从原始指标到根因报告全程 LLM 驱动。

这里的“全程 LLM 驱动”并不否认 Triage 的确定性实现，而是强调最终诊断裁决不依赖人工编写的阈值规则，不需要研究者手工定义“多少条 strong 证据才算通过”。

---

## Part III：Stage 1 — Triage

Triage 是纯确定性 Python 代码，不调用 LLM，不消耗 token。职责：从 150+ 原始指标中筛选 Top-K 异常指标，输出带时序因果先验的 `FocusContext`。

### 3.1 处理流程（3 步）

#### Step 1：异常指标筛选（Anomaly Scoring）

对每个指标列执行：

- **数据自适应基线**：使用实验自身时序数据计算统计基线（前 N 个采样点的均值/标准差，或整体滑动窗口统计量），不依赖外部预计算的 Baseline Profile
- **Z-score 异常评分**：相对于数据自适应基线计算 Z-score
- **持久性过滤**：连续超阈值比例未达到持久性要求的指标剔除，去除毛刺
- **变化点检测**：对候选异常指标做 PELT 或 BOCPD，标注 `change_point`，精确定位 `t_onset`

输出：Top-K 异常指标，每个指标附带 `score`、`direction`、`t_onset`。

#### Step 2：时序因果排序（Temporal Ordering）

- 按 `t_onset` 升序排列异常指标，生成 `causal_order`
- 按 Metric KB 的 `subsystem` 字段分组，计算 `subsystem_scores`
- 确定 `leading_subsystem`

#### Step 3：FocusContext 构建

- 将 Top-K 指标、causal_order、subsystem_scores 聚合
- 与 `jobinfo.csv` 做时间窗口交集，关联受影响的作业和节点
- 计算 `triage_confidence`

### 3.2 FocusContext Schema

```json
{
  "run_id": "string",
  "anomaly_window": {"start": "ISO8601", "end": "ISO8601"},
  "top_metrics": [
    {
      "metric": "string",
      "subsystem": "string",
      "direction": "+|-",
      "score": 0.0,
      "t_onset": "ISO8601",
      "onset_rank": 1,
      "change_point": "ISO8601|null"
    }
  ],
  "causal_order": ["metric_A", "metric_B", "metric_C"],
  "subsystem_scores": {"cpu": 0.0, "memory": 0.0, "network": 0.0, "disk": 0.0},
  "leading_subsystem": "string",
  "nodes": [{"node": "string", "severity": "high|medium|low"}],
  "jobs": [{"job_id": "string", "overlap_ratio": 0.0, "node_set": []}],
  "triage_confidence": 0.0
}
```

关键字段：

- `causal_order`：按 `t_onset` 排序的指标列表，是时序因果先验
- `leading_subsystem`：分诊初判的首发子系统
- `triage_confidence`：分诊可信度，低时 HYPOTHESIZE 会生成更多候选假设

---

## Part IV：Stage 2 — Diagnosis Agent

### 4.1 AgentState Schema

```python
class AgentState(TypedDict):
    run_id: str
    inputs: dict                    # {metrics_path, jobinfo_path}
    focus_context: FocusContext     # Stage 1 产物
    hypotheses: list[Hypothesis]    # 多假设并行
    evidence: list[Evidence]        # 主 Agent 收集的证据池
    react_trace: list[ReActStep]    # THINK/ACT/OBSERVE 轨迹
    gate_evidence: list[GateEvidence]  # GATE Sub-Agent 独立证据池
    gate_trace: list[GateStep]      # GATE_THINK/ACT/OBSERVE 轨迹
    gate_hint: str | None           # GATE 传回 THINK 的下一步提示
    budget: dict                    # {tool_calls_used, tool_calls_limit, tokens_in, tokens_out}
    gate_budget: dict               # {tool_calls_used, tool_calls_limit, max_rounds}
    rehyp_count: int                # 重新假设次数
    gate_result: dict | None        # {decision, reason, hint, diagnosis_type}
    report: DiagnosisReport | None
```

设计要点：

- `evidence` 与 `gate_evidence` 严格分离，避免 GATE 的主动调查结果被误当成 THINK 自身的证据积累
- `gate_hint` 是方案 C 的关键桥接字段，只承载“下一步该查什么”，不直接传递 GATE 的结论
- `gate_budget` 与 `budget` 分离，使主诊断探索与审查开销可以单独统计和消融

### 4.2 Hypothesis / Evidence Schema

```python
class Hypothesis(BaseModel):
    id: str
    root_cause: str
    fault_type: str
    subsystem: str
    prior_confidence: float
    current_confidence: float
    required_verifications: list[VerificationItem]
    status: Literal["active", "confirmed", "refuted", "derived"]

class Evidence(BaseModel):
    id: str
    hypothesis_ids: list[str]
    type: Literal["supporting", "refuting", "neutral", "mixed"]
    source_tool: str
    query_summary: str
    result_digest: str
    raw_stats: dict

class GateEvidence(BaseModel):
    id: str
    source_tool: str
    query_summary: str
    result_digest: str
    raw_stats: dict
    target_hypothesis_ids: list[str]
    purpose: str
```

与 v5 的关键差异：

- `Evidence.strength` 字段被移除
- OBSERVE 不再给证据打 `strong / medium / weak` 标签
- `raw_stats` 保留，供 GATE LLM 根据上下文自主判断证据质量

### 4.3 LangGraph 节点与路由

#### 节点一览

| 节点 | 类型 | 职责 |
|------|------|------|
| HYPOTHESIZE | LLM | 基于 FocusContext + FPL Top-K 命中生成 2–4 个候选假设 |
| THINK | LLM (ReAct) | 结合证据状态与 `gate_hint` 决定下一步调查或尝试下结论 |
| ACT | 确定性 | 执行工具调用，记录资源消耗 |
| OBSERVE | 确定性 | 压缩工具输出、生成 `result_digest`、提取 `raw_stats`、更新 verification 状态 |
| GATE | LLM Sub-Agent | 独立审查 THINK 的结论，必要时主动补查漏洞 |
| FINALIZE | LLM | 生成最终 DiagnosisReport |

#### 主图路由

```text
                    HYPOTHESIZE
                         │
                         ▼
                 ┌──── THINK ◄──────────────────────┐
                 │      │                            │
                 │  action.type?                     │
                 │  ┌─────┴──────┐                   │
                 ▼  ▼            ▼                   │
            [tool_call]      [conclude]              │
                 │              │                    │
                 ▼              ▼                    │
                ACT         GATE Sub-Agent           │
                 │      ┌────┼────┬───────────┐      │
                 ▼      │    │    │           │      │
             OBSERVE    │    │    │           │      │
                 │      │    │    │           │      │
                 └──────┘    │    │           │      │
                    ▲        │    │           │      │
                    └────────┘    │           │      │
                               [pass]   [degrade]    │
                                  │         │        │
                                  ▼         ▼        │
                               FINALIZE  FINALIZE    │
                                            ▲        │
                              [continue]────┘        │
                              [rehypothesize]───────►HYPOTHESIZE
```

#### 路由规则

| 来源 | 条件 | 目标 |
|------|------|------|
| HYPOTHESIZE | 完成 | → THINK |
| THINK | `action.type = "tool_call"` | → ACT |
| THINK | `action.type = "conclude"` | → GATE |
| ACT | 完成 | → OBSERVE |
| OBSERVE | 完成 | → THINK |
| GATE | `decision = "pass"` | → FINALIZE |
| GATE | `decision = "continue"` | → THINK |
| GATE | `decision = "rehypothesize"` | → HYPOTHESIZE |
| GATE | `decision = "degrade"` | → FINALIZE |

权责分离：THINK 回答“我是否准备好下结论”；GATE 回答“这个结论是否经得起独立审查”。两者都由 LLM 执行，但信息可见范围不同。

#### THINK 节点输出格式

```json
{
  "thought": "e1 和 e2 说明 CPU 异常早于 memory 异常，h2 正在被削弱...",
  "action": {
    "type": "tool_call | conclude",
    "tool": "MetricQueryTool",
    "args": {},
    "reasoning": "需要验证 h1 的 verification[1]"
  }
}
```

### 4.4 HYPOTHESIZE 重新进入行为

当 GATE 触发 `rehypothesize` 时，HYPOTHESIZE 节点被第二次调用。此时 state 中已包含：

- refuted 假设列表及其驳回理由
- 第一轮主 Agent 已收集的 evidence
- FocusContext 原始结构化摘要

HYPOTHESIZE 的 prompt 在 `rehyp_count > 0` 时包含额外指令：

> 以下候选假设已经被前一轮审查否定，请不要重复这些 fault_type。请基于未被解释的异常指标和现有证据空白生成新的候选假设。

设计约束：

- 新假设 ID 续编（如 h5, h6）
- 旧假设保留在 `hypotheses` 列表中，`status = refuted`
- 新假设不得与已 refuted 假设的 `fault_type` 重复

### 4.5 复合故障支持

`AgentState.hypotheses` 可同时维护多个互不排斥的假设。THINK 在每轮对所有 active 假设进行比较，优先验证：

1. 与 `leading_subsystem` 更一致的假设
2. 所需 verification gap 更小的假设
3. 能解释更多 top_metrics 的假设

`diagnosis_type` 的定义：

- `single_fault`：恰好 1 个 confirmed，无 active
- `composite_fault`：≥2 个独立 confirmed，均经 GATE 甄别后保留
- `partial`：≥1 个 confirmed，但 budget 耗尽，仍有 active
- `inconclusive`：无 confirmed，或全部 refuted 且无法继续

### 4.6 工具契约

#### MetricQueryTool

```text
输入：nodes, metrics, time_window, aggregation (mean|p95|max|duration_above_threshold)
输出：results [{node, metric, value, unit}], missing [{node, metric, reason}]
注：LLM 接收的是聚合摘要，不是原始序列
```

#### KBRetrievalTool

```text
输入 mode A — metric_lookup：按指标名查询 Metric KB 条目
输入 mode B — pattern_match：按症状签名查询 FPL（子系统 + 异常指标 + causal_order）
输出：metric_entries[] 或 pattern_hits[]
```

#### DataAnalysisTool

```text
输入：analysis_type (correlation | changepoint | group_compare | lag_analysis)
输出：findings [{statistic_name, value, interpretation}], summary
注：禁止返回原始序列，只返回结构化统计量和文字摘要
```

### 4.7 GATE Sub-Agent 详细设计

GATE 不是一个简单的“判分节点”，而是一个受控的对抗审查子图。它的设计目标不是自行生成诊断，而是在 THINK 已提出结论时主动寻找反例、逻辑空白或未排除的替代解释。

#### 角色定义

- **身份**：Evidence Reviewer / Adversarial Auditor
- **目标**：尽可能推翻 THINK 当前结论；若找不到漏洞，才放行
- **工作原则**：先找逻辑缺口，再决定是否需要额外工具调用

#### GATE 可见信息

GATE 输入上下文只包含：

- `focus_context`
- `hypotheses`
- `evidence`
- `gate_hint` 的历史值（如果实现上保留最近一次）
- Metric KB 与 FPL 的必要检索结果

GATE 不可见：

- `react_trace.thought`
- THINK 的完整 reasoning chain
- 主 Agent 的 prompt 内部模板

这实现了信息隔离，避免 GATE 顺着 THINK 的推理链走向“默认认同”。

#### GATE System Prompt 结构

GATE system prompt 建议由四段组成：

1. **角色声明**：你是证据审查员，不负责给出诊断，只负责寻找漏洞
2. **审查原则**：
   - 单一现象不能被重复包装成多条支持证据
   - 替代假设未排除时不得放行
   - 强反驳证据优先于支持性描述
   - 多根因情形必须区分独立故障与下游症状
3. **工具使用约束**：只有在逻辑检查不能直接得出裁决时才允许调用工具；最多 3 次
4. **输出要求**：必须返回 `decision`、`reason`、`hint`

#### GATE 内部 ReAct 循环

```text
输入：结论候选 + evidence[] + hypotheses[]
    │
    ▼
GATE_THINK
  - 先做无工具的逻辑审查
  - 若已发现明确漏洞：continue / rehypothesize
  - 若已确认没有漏洞：pass
  - 若仍有关键不确定点：发起 gate_tool_call
    │
    ▼
GATE_ACT
  - 执行受限工具调用
    │
    ▼
GATE_OBSERVE
  - 生成 gate_evidence
  - 回到 GATE_THINK
```

#### 工具约束

GATE 只允许使用：

- `MetricQueryTool`
- `KBRetrievalTool`

默认不允许使用 `DataAnalysisTool`，原因是：

- DataAnalysisTool 容易将 GATE 变成第二个诊断者，而不是审查者
- GATE 的职责是验证漏洞，不是重新跑一套完整分析

#### Budget 配置

```yaml
gate_budget:
  tool_calls_limit: 3
  max_rounds: 2
```

解释：

- 大多数 continue 场景可以在第一轮逻辑审查直接发现问题
- 少数需要补查的场景通常 1–2 次工具调用足够
- 若给 GATE 太大 budget，它会退化为第二个 diagnosis agent，破坏架构清晰性

#### 四种决策的语义

- `pass`：GATE 未发现仍未解释的关键漏洞，允许 FINALIZE
- `continue`：当前证据不足以支持结论，但仍有明确可补查方向
- `rehypothesize`：现有假设空间整体失效，继续补查已无意义，需要重开假设
- `degrade`：无论补查还是重开都缺乏预算或可靠性，不再继续

#### 快路径

GATE 的第一轮逻辑审查允许零工具直接结束：

- **零工具 continue**：例如只有一条 CPU 升高证据，但 memory/network 替代假设均未排除
- **零工具 pass**：例如 THINK 已提供三条独立证据且所有替代假设均被直接反驳

#### hint 语义规范

`hint` 只在 `decision = continue` 时必填，要求满足：

- 是可执行的问题，而不是笼统抱怨
- 指向一个或两个具体验证缺口
- 不直接包含 GATE 自己的最终判断

例子：

- 好的 hint：`请验证 memory 异常是否先于 cpu 异常出现，并检查 swap 是否同步上升`
- 差的 hint：`你的结论不对，重新想`

#### 终止条件

GATE 在满足任一条件时结束：

1. 找到足以推迟放行的关键漏洞
2. 找不到任何关键漏洞
3. 已达 `tool_calls_limit`
4. 已达 `max_rounds`

#### 输出 Schema

```json
{
  "decision": "pass | continue | rehypothesize | degrade",
  "reason": "说明裁决依据",
  "hint": "下一步该验证什么；仅 continue 时非空",
  "diagnosis_type": "single_fault | composite_fault | partial | inconclusive | null"
}
```

### 4.8 OBSERVE 节点详细设计

OBSERVE 在 v6 中刻意收窄职责，不再裁决证据强弱，只做以下四件事：

1. **结果压缩**：把工具输出转成 1–2 句 `result_digest`
2. **统计量提取**：把关键数值放进 `raw_stats`
3. **verification 对齐**：标记哪些 verification item 被触达
4. **异常处理**：把工具超时、缺失数据、空结果编码成结构化状态

OBSERVE 明确不做：

- 不根据 `strength_thresholds` 打标签
- 不决定假设 confirmed 或 refuted
- 不决定是否进入 FINALIZE

这使 OBSERVE 成为一个“上下文压缩器”，而不是“半个裁判”。

### 4.9 THINK-GATE 交互协议

v6 采用方案 C：GATE 的调查结果不直接写回主证据池，而是通过 `gate_hint` 对 THINK 形成最小必要约束。

#### 协议流程

1. THINK 触发 `conclude`
2. GATE 独立审查并返回 `decision`
3. 若 `decision = continue`：
   - `gate_hint` 写入 state
   - `gate_evidence` 保留在独立字段
   - THINK 下一轮必须优先响应 `gate_hint`
4. 若 `decision = pass` 或 `degrade`：
   - `gate_hint = null`

#### THINK 的响应规则

THINK 读取到 `gate_hint` 时：

- 下一轮优先围绕 hint 补查
- 若 hint 指向多个缺口，可自主排序
- 完成 hint 所需验证后，才允许再次 `conclude`

#### 方案 C 的优势

- **保留角色边界**：GATE 不直接替 THINK 做工作
- **减少重复劳动**：THINK 不必再次从零发现漏洞
- **便于审计**：用户能清晰看到“GATE 提了什么问题，THINK 如何响应”

---

## Part V：Knowledge Base

### 5.1 两层结构

知识库分为两层，在系统启动前离线构建一次：

| 层次 | 存储格式 | 内容 |
|------|---------|------|
| **Metric KB** | YAML + ChromaDB 向量索引 | 指标语义描述、正常范围、`strength_thresholds`、`downstream_effects` |
| **Fault Pattern Library (FPL)** | JSONL | 故障模式规则，初始为空，通过 Reflect 积累 |

### 5.2 Metric KB 条目规格

```yaml
- name: "cpu_usage_percent"
  subsystem: "cpu"
  description: "面向诊断的语义描述"
  unit: "%"
  normal_range: {min, max}
  strength_thresholds:
    weak:   {condition, min_duration_sec}
    medium: {condition, min_duration_sec}
    strong: {condition, min_duration_sec}
  related_faults: ["cpu_fullload"]
  downstream_effects:
    - metric: "memory_usage_percent"
      lag_range_sec: [120, 600]
      condition: "if_sustained"
```

关键设计：

- `strength_thresholds` 不再由 OBSERVE 确定性执行，而是供 GATE 作为参考文档理解“这个指标通常何时算强信号”
- `downstream_effects` 仍是复合故障甄别的重要参考，用于判断某异常是否更像继发症状
- `related_faults` 在 FPL 冷启动阶段帮助 HYPOTHESIZE 枚举 plausible fault types

### 5.3 FPL 条目规格

```json
{
  "pattern_id": "fpl_xxx",
  "fault_type": "cpu_fullload",
  "version": 1,
  "status": "candidate | confirmed | deprecated",
  "source": "learned",
  "provenance_exp_ids": [],
  "confidence": 0.5,
  "symptom_signature": {
    "leading_subsystem": "cpu",
    "required_metrics": [],
    "optional_metrics": [],
    "temporal_pattern": "描述时序特征",
    "causal_order_hint": []
  },
  "verification_steps": [],
  "solutions": []
}
```

规则生命周期：

1. Reflect 生成候选规则，初始 `status = candidate`
2. 后续被多次命中并通过诊断确认后升级为 `confirmed`
3. 表现恶化后降级为 `deprecated`

---

## Part VI：Evidence Gating

Evidence Gating 是 v6 的核心升级点。它不再是一个人工写死的规则函数，而是一个被强约束的 Adversarial Sub-Agent。其目标不是“机械打分”，而是“以反方视角寻找结论漏洞”。

### 6.1 三项核心原理

**原理 1：证据独立性**

GATE 会检查支持性证据是否只是同一现象的重复描述。例如，同一轮 CPU 查询产生的多个聚合统计不应被当成多条独立证据。

**原理 2：反驳优先**

一条强有力的反驳线索足以阻止通过，即使支持性证据很多。GATE 会优先寻找“解释不了的反例”而不是累加支持点数。

**原理 3：复合故障甄别**

当多个假设都看似成立时，GATE 需要区分真正的独立根因和下游症状链。它主要借助 `causal_order`、`downstream_effects` 与异常幅度/时滞进行判断。

### 6.2 GATE 的三阶段思考框架

虽然 v6 不再把 Gate 实现成 Python 三阶段规则，但 GATE 的 system prompt 仍要求它按以下顺序思考：

```text
Stage A: 单假设审查
  - 当前结论的核心支持证据是什么？
  - 有哪些替代解释尚未排除？
  - 是否存在直接反驳线索？

Stage B: 多假设关系审查
  - 若多个假设同时成立，它们是独立故障还是因果链？
  - 哪些异常更像 derived symptoms？

Stage C: 全局裁决
  - pass / continue / rehypothesize / degrade
```

### 6.3 GATE 的裁决逻辑

#### `pass`

适用条件：

- 关键替代解释已被排除
- 主要异常都有解释归属
- 不存在明显未验证的关键缺口

#### `continue`

适用条件：

- 当前结论有合理性，但仍存在可补查的关键漏洞
- 漏洞能被 1–2 个额外查询验证

`continue` 的本质不是“失败”，而是“尚不足以放行”。

#### `rehypothesize`

适用条件：

- 当前假设集合整体无法解释观测
- 继续在现有假设上补查已无明显价值
- 仍有足够预算允许重开假设空间

#### `degrade`

适用条件：

- 预算耗尽
- 数据严重缺失
- 审查发现问题，但已不足以支撑继续推进

`degrade` 不是“系统失败”，而是诚实地输出部分诊断或不确定诊断。

### 6.4 零人工阈值的含义

v6 不是完全没有数值信息，而是不把数值阈值写成“若满足 X 条件则 pass”的人工规则。数值仍然存在于：

- `raw_stats`
- Metric KB 的 `strength_thresholds`
- `downstream_effects`

但这些都只是 GATE 的审查依据，而不是外部研究者手工编写的强制裁决逻辑。

### 6.5 为什么这比规则 Gate 更适合论文 Claim

规则 Gate 会引出一个很难回避的问题：到底是谁定义了“2 条 strong 证据才算充分”？这会削弱“完全脱手”的 claim。v6 的 GATE 通过角色分离、信息隔离和结构化输出，把“是否通过”的判断交给受控 LLM，而不是人工阈值工程。

---

## Part VII：Reflect

FINALIZE 完成后按条件触发 Reflect。核心职责：将本次诊断经验提炼为 FPL 规则，实现无监督知识积累。

### 7.1 触发前置条件

Reflect 不是无条件触发，仅在有可信 confirmed 根因时才有可提炼信号：

| diagnosis_type | Reflect 策略 |
|---------------|--------------|
| `single_fault` / `composite_fault` | 完整触发，提炼所有 confirmed 假设 |
| `partial` | 部分触发，只提炼 confirmed 假设，且标记 `incomplete = true` |
| `inconclusive` | 跳过 |

### 7.2 执行步骤

1. **规则提炼**：LLM 分析 confirmed 假设的 symptom signature 与 verification path，生成候选规则
2. **去重写入**：与已有 FPL 做相似度比较，相似则更新版本，不相似则新增
3. **规则升级 / 衰减**：根据后续命中表现调整 `confidence` 和 `status`

### 7.3 FPL 冷启动行为

FPL 为空时，系统仍然可运行：

- `prior_confidence` 使用默认值 0.30
- `verification_steps` 缺省，由 THINK 自主探索
- `related_faults` 与 `causal_order` 仍可支撑初始假设生成

### 7.4 防污染措施

- LLM 只生成 schema 允许的字段
- `confidence` 由系统根据后续表现更新，不允许 LLM 自行填写最终数值
- 写入前做 Pydantic 校验

---

## Part VIII：评估方案

### 8.1 Ground Truth 比对（独立于 Reflect）

在实验评估模式下，使用 `chaos.csv` 作为 Ground Truth：

- 对比 `DiagnosisReport.root_causes` 与实际注入故障
- 计算 Hit@1、Hit@3、Composite Coverage
- 评估结果只用于分析，不回流进入 Reflect

### 8.2 数据集

| 数据集 | 规模 | 用途 |
|--------|------|------|
| formaltest | 29 实验 | 主评测 |
| compositetest_backup | 8 实验 | 复合故障补充评测 |

### 8.3 评估指标

| 类别 | 指标 | 说明 |
|------|------|------|
| 准确性 | Root Cause Hit@1 | Top-1 根因匹配率 |
| 准确性 | Root Cause Hit@3 | Top-3 覆盖率 |
| 准确性 | Composite Coverage | 复合故障完整诊断比例 |
| 效率 | Avg Tool Calls | 平均工具调用次数 |
| 效率 | Avg Gate Tool Calls | GATE 平均额外工具调用次数 |
| 效率 | End-to-end Latency | 总耗时 |
| 成本 | Token Usage | THINK / GATE / FINALIZE 分项 token |
| 鲁棒性 | Hit@1 @ 20% missing | 缺失指标下性能退化 |
| KB 增益 | ΔHit@1 (with/without FPL) | 知识积累收益 |

---

## Part IX：DiagnosisReport Schema

```json
{
  "run_id": "string",
  "anomaly_summary": "string",
  "diagnosis_type": "single_fault | composite_fault | partial | inconclusive",
  "root_causes": [
    {
      "cause": "CPU fullload on blade01",
      "fault_type": "cpu_fullload",
      "confidence": 0.87,
      "evidence_ids": ["e1", "e2"],
      "counter_evidence_ids": [],
      "fpl_pattern_id": "fpl_xxx | null"
    }
  ],
  "derived_symptoms": [
    {
      "symptom": "elevated load average",
      "caused_by_root_cause_id": 0
    }
  ],
  "solutions": [
    {
      "action": "identify and kill runaway process",
      "rationale": "CPU fullload confirmed by multiple sources",
      "risk": "low",
      "verification": "monitor cpu_usage drop after action"
    }
  ],
  "trace_summary": {
    "triage_leading_subsystem": "cpu",
    "main_tools_used": ["MetricQueryTool x3", "DataAnalysisTool x1"],
    "gate_tools_used": ["KBRetrievalTool x1"],
    "total_tool_calls": 5,
    "main_iterations": 4,
    "gate_iterations": 1
  },
  "uncertainties": []
}
```

v6 相比 v5 的变化：

- 主 Agent 与 GATE 的消耗分开记录
- 便于回答“GATE 到底做了多少额外工作”这一消融问题

---

## Part X：消融实验

### 10.1 主消融矩阵（4 ablation + 2 baseline）

| 编号 | 名称 | 消融方式 | 验证的设计贡献 |
|------|------|---------|--------------|
| Full | 完整系统 | 无消融 | 性能上限 |
| Abl-A | 无 Triage | 去掉 Stage 1，全量摘要直接输入 HYPOTHESIZE | Hybrid Control |
| Abl-B | 无 GATE | THINK 输出 conclude 后直接 FINALIZE | Adversarial Evidence Gating |
| Abl-C | 无 FPL | pattern_match 恒为空 | Knowledge Accumulation |
| BL-1 | 单次 LLM 调用 | Phase 2 基线 | Agent vs 单次调用 |
| BL-2 | 纯统计规则 | 只 Triage + 规则匹配 | LLM 推理必要性 |

### 10.2 Supplementary 实验

| 编号 | 名称 | 消融方式 | 说明 |
|------|------|---------|------|
| Sup-A | 无时序因果先验 | 打乱 `causal_order` | 验证 Triage 内部贡献 |
| Sup-B | 无 Reflect | FPL 不更新 | 验证知识积累收益 |
| Sup-C | 无 Re-hypothesis | 禁用 `rehypothesize` | 验证纠错能力 |
| Sup-D | GATE 降级为单次 LLM call | 无工具、无循环，仅一次审查 | 验证 GATE 主动调查的贡献 |

---

## Part XI：失败处理

| 场景 | 处理策略 |
|------|---------|
| 主工具超时 | 生成不可用证据，禁止作为支持性结论依据 |
| GATE 工具超时 | GATE 可选择 `continue` 或 `degrade`，不得凭空补完证据 |
| 工具返回空数据 | 对应 verification 标记为 `unverifiable` |
| THINK 输出不符合 Schema | 最多重试 2 次，仍失败则降级输出 |
| GATE 输出不符合 Schema | 最多重试 2 次，仍失败则退化为 `degrade` |
| Budget 耗尽且已有 confirmed | 输出 `partial` |
| Budget 耗尽且无 confirmed | 输出 `inconclusive` |
| 全部假设 refuted 且可重开 | 触发 `rehypothesize` |
| 全部假设 refuted 且不可重开 | `degrade` |
| FPL 命中但验证失败 | 规则只作为先验，不允许直接下结论 |

---

## Part XII：实现路线

| 阶段 | 内容 | 关键产物 |
|------|------|---------|
| Phase A | Metric KB + FPL 初始构建 | `kb/metrics.yaml`, `kb/fpl.jsonl` |
| Phase B | Triage Stage：异常评分 + causal_order + FocusContext | `agent/triage.py` |
| Phase C | Diagnosis Agent：主 ReAct + GATE Sub-Agent + tools | `agent/diagnosis.py`, `agent/tools/`, `agent/gate.py` |
| Phase D | Reflect + FPL 写回 + 评估框架 | `agent/reflect.py`, `eval/evaluate.py` |
| Phase E | 消融实验 + 论文写作 | 实验报告与论文材料 |

Phase C 的实现建议拆成三步：

1. 先实现主图（HYPOTHESIZE / THINK / ACT / OBSERVE / FINALIZE）
2. 再加入最小版 GATE（单轮、零工具）验证路由
3. 最后扩展成完整 GATE Sub-Agent（含工具调用与方案 C 的 hint 协议）

---

## Part XIII：一次完整诊断的控制流示例

为了让实现与论文叙事统一，v6 推荐在附录或实现文档中固定使用如下控制流叙事：

1. Triage 生成 `FocusContext`
2. HYPOTHESIZE 生成 2–4 个候选假设
3. THINK 进入主 ReAct 循环，逐步收集 supporting / refuting evidence
4. THINK 在认为证据充分时触发 `conclude`
5. GATE 先做零工具逻辑审查
6. 若发现缺口，返回 `continue + hint`
7. THINK 根据 `gate_hint` 继续补查
8. 若当前假设空间失效，GATE 返回 `rehypothesize`
9. 若找不到漏洞，GATE 返回 `pass`
10. FINALIZE 生成 DiagnosisReport
11. Reflect 在符合条件时回写 FPL

这个控制流比 v5 更强，因为它显式展示了“诊断者提出主张，审查者独立质疑，再决定是否放行”的闭环。